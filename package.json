{
  "name": "cesium-helpers",
  "private": true,
  "scripts": {
    "build-demos": "vite build",
    "dev": "vite",
    "gh-pages": "rm -rf dist && npm run build-demos && gh-pages -d dist"
  },
  "devDependencies": {
    "@cesium/engine": "26.0.0",
    "gh-pages": "6.3.0",
    "lerna": "10.0.1",
    "lit": "3.3.3",
    "vite": "^8.1.0"
  }
}
